# FluidMarkdown 特殊显示功能完整示例

这个文档展示了 FluidMarkdown 库支持的所有特殊显示类型和功能。

## 📐 数学公式支持

### 内联数学公式
这是一个内联数学公式：$E = mc^2$，爱因斯坦的质能方程。

更复杂的内联公式：$\sum_{i=1}^{n} x_i = x_1 + x_2 + \cdots + x_n$

### 块级数学公式

经典的高斯积分：
$$
\int_{-\infty}^{+\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$

纳维-斯托克斯方程（流体力学）：
$$
\rho \left( \frac{\partial \mathbf{u}}{\partial t} + \mathbf{u} \cdot \nabla \mathbf{u} \right) = -\nabla p + \mu \nabla^2 \mathbf{u} + \mathbf{f}
$$

麦克斯韦方程组：
$$
\begin{align}
\nabla \cdot \mathbf{E} &= \frac{\rho}{\epsilon_0} \\
\nabla \cdot \mathbf{B} &= 0 \\
\nabla \times \mathbf{E} &= -\frac{\partial \mathbf{B}}{\partial t} \\
\nabla \times \mathbf{B} &= \mu_0 \mathbf{J} + \mu_0 \epsilon_0 \frac{\partial \mathbf{E}}{\partial t}
\end{align}
$$

## 🔗 链接高亮功能

### 普通链接
这是一个普通的链接：[FluidMarkdown 官方文档](https://github.com/FluidMarkdown/FluidMarkdown)

这是另一个链接：[苹果开发者文档](https://developer.apple.com/documentation/)


## 💻 代码高亮

### 内联代码
这是内联代码：`NSString *text = @"Hello World";`

使用 `UIColor.randomColor` 方法可以生成随机颜色。

### 代码块

#### Swift 代码示例
```swift
import UIKit

class ViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        
        let label = UILabel()
        label.text = "Hello, FluidMarkdown!"
        label.textColor = UIColor.systemBlue
        view.addSubview(label)
    }
}
```

#### Objective-C 代码示例
```objc
#import "UIColor+Random.h"

@implementation UIColor (Random)

+ (UIColor *)randomColor {
    CGFloat red = arc4random_uniform(256) / 255.0;
    CGFloat green = arc4random_uniform(256) / 255.0;
    CGFloat blue = arc4random_uniform(256) / 255.0;
    return [UIColor colorWithRed:red green:green blue:blue alpha:1.0];
}

@end
```

#### JavaScript 代码示例
```javascript
function highlightCode(element) {
    const code = element.textContent;
    const highlighted = Prism.highlight(code, Prism.languages.javascript, 'javascript');
    element.innerHTML = highlighted;
}
```

#### Python 代码示例
```python
import numpy as np
import matplotlib.pyplot as plt

def plot_function(x, y):
    plt.figure(figsize=(10, 6))
    plt.plot(x, y, 'b-', linewidth=2)
    plt.xlabel('X axis')
    plt.ylabel('Y axis')
    plt.title('Function Plot')
    plt.grid(True)
    plt.show()
```

## 📊 表格显示

### 基础表格
| 功能类型 | 支持状态 | 描述 |
|---------|---------|------|
| 数学公式 | ✅ | 支持内联和块级 LaTeX 公式 |
| 代码高亮 | ✅ | 支持多种编程语言语法高亮 |
| 链接高亮 | ✅ | 支持普通链接和图标链接 |
| 表格渲染 | ✅ | 支持复杂表格显示和交互 |

### 复杂表格示例
| 编程语言 | 类型 | 特点 | 应用场景 | 学习难度 |
|---------|------|------|---------|---------|
| Swift | 编译型 | 类型安全、性能优秀 | iOS/macOS 开发 | 中等 |
| Objective-C | 编译型 | 动态特性、C 语言超集 | iOS/macOS 开发 | 较难 |
| JavaScript | 解释型 | 灵活、事件驱动 | Web 前端开发 | 简单 |
| Python | 解释型 | 语法简洁、库丰富 | 数据科学、AI | 简单 |

## 🎨 文本样式

### 基础文本样式
这是 **粗体文本**，这是 *斜体文本*，这是 ***粗斜体文本***。

这是 ~~删除线文本~~，这是 <u>下划线文本</u>。

### 高亮样式
这是 <span class="highlight">高亮文本</span>，用于强调重要内容。

这是 <span class="poi">POI 标记文本</span>，用于标记特殊位置。

### 颜色标记
这是 <span class="markdown-red-color">红色文本</span>，表示警告或错误。

这是 <span class="markdown-green-color">绿色文本</span>，表示成功或正确。

## 📝 列表功能

### 有序列表
1. 第一项内容
   - 子项 A
   - 子项 B
2. 第二项内容
   * 子项 C
   + 子项 D
3. 第三项内容

### 无序列表
- 项目管理
  1. 需求分析
  2. 设计阶段
  3. 开发实现
  4. 测试验证
- 技术栈选择
  - 前端框架
  - 后端服务
  - 数据库设计

### 任务列表
- [x] 完成数学公式支持
- [x] 实现代码高亮功能
- [x] 添加链接高亮
- [ ] 优化表格渲染性能
- [ ] 增加图片懒加载

## 📖 引用块

> 这是一个简单的引用块。
> 
> 引用块可以包含多行内容，用于展示重要的引用或说明。

> ### 嵌套引用示例
> 
> 这是外层引用。
> 
> > 这是内层引用，可以嵌套使用。
> > 
> > 支持在引用中使用其他 Markdown 语法。

## 🔧 特殊功能

### 脚注支持
这是一个包含脚注的文本[^1]，点击可以查看详细说明。

另一个脚注示例[^note]，展示不同的脚注格式。

[^1]: 这是第一个脚注的内容，提供额外的说明信息。

[^note]: 这是命名脚注，可以使用有意义的名称。

### 分隔线
---

### 转义字符
使用反斜杠可以转义特殊字符：\*不是斜体\*，\`不是代码\`。

## 🎯 总结

FluidMarkdown 库提供了丰富的特殊显示功能：

1. **数学公式**：完整的 LaTeX 支持
2. **代码高亮**：多语言语法高亮
3. **链接功能**：普通链接和图标链接
4. **表格渲染**：复杂表格支持
5. **文本样式**：丰富的样式选项
6. **列表功能**：有序、无序、任务列表
7. **引用块**：支持嵌套引用
8. **特殊功能**：脚注、分隔线等

这些功能使得 FluidMarkdown 成为一个功能完整、表现力强的 Markdown 渲染库。
